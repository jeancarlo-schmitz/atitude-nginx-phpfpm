server {
    listen 80;
    server_name api.recuperajur.com.br;
    
    # ACME challenge (Let's Encrypt) - caminho corrigido
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
        default_type "text/plain";
    }
    
    # Temporariamente comente o redirect para debugar
    # Depois que o SSL funcionar, descomente esta linha:
    # return 301 https://$host$request_uri;
    
    # Temporariamente, deixe responder em HTTP para testar
    root /var/www/html/atitude-recuperajur-service/public;
    index index.php;
    
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    
    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass php:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_index index.php;
        fastcgi_read_timeout 300;
    }
}

# Comente temporariamente o bloco HTTPS até obter o certificado
# server {
#     listen 443 ssl http2;
#     server_name api.recuperajur.com.br;
#     
#     root /var/www/html/atitude-recuperajur-service/public;
#     index index.php;
#     
#     ssl_certificate /etc/letsencrypt/live/api.recuperajur.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.recuperajur.com.br/privkey.pem;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES128-SHA:ECDHE-RSA-AES256-SHA;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#     
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass php:9000;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_index index.php;
#         fastcgi_read_timeout 300;
#     }
#     
#     location ~ /\.env {
#         deny all;
#         return 404;
#     }
#     
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header X-Frame-Options "DENY" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     
#     location ~ /\. {
#         deny all;
#         return 404;
#     }
# }